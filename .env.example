# Backend Environment Variables (for server/.env or Render)
DATABASE_URL=postgresql://user:password@host:5432/postgres
SESSION_SECRET=your-secret-key-here-change-this-in-production
NODE_ENV=production

# Frontend Environment Variables (for client/.env.production or .env)
# Only set this if backend is on a different domain (e.g., Render)
# Leave empty or unset for same-origin requests (localhost:5000)
VITE_API_URL=https://your-render-app.onrender.com
